# Local override: expose ClickHouse HTTP on loopback ONLY for the agent/MCP.
# Copy to docker-compose.override.yml (picked up automatically).
# The port stays closed externally — it listens on 127.0.0.1 only.
services:
  clickhouse:
    ports:
      - "127.0.0.1:8123:8123"
