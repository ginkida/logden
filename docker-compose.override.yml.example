# Локальный оверрайд: открыть ClickHouse HTTP ТОЛЬКО на loopback для агента/MCP.
# Скопируйте в docker-compose.override.yml (подхватывается автоматически).
# Наружу порт по-прежнему закрыт — слушает только 127.0.0.1.
services:
  clickhouse:
    ports:
      - "127.0.0.1:8123:8123"
